MD003: false  # heading-style
MD013: false
MD024: false  # /no-duplicate-heading: Multiple headings with the same content
MD025: false  # Multiple top-level headings in the same documentmarkdownlint
MD029: false
MD034: false  # Bare URL usedmarkdownlint
MD040: false  # /fenced-code-language: Fenced code blocks should have a language specified
MD036: false  # /no-emphasis-as-heading: Emphasis used instead of a heading
MD041: false  # /first-line-heading/first-line-h1: First line in a file should be a top-level
MD046: false  # /code-block-style: Code block style [Expected: indented; Actual: fenced]
MD060: false  # /table-column-style: Table column style [Table pipe does not align with header for style "aligned"]
